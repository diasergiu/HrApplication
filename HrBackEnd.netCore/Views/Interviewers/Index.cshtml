@model IEnumerable<HrBackEnd.netCore.Entitys.Interviewer>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.interviewerName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.interviewerName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.idInterviewer">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idInterviewer">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idInterviewer">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
